# =============================================================================
# DATABASE
# =============================================================================
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/microservices?schema=public

# =============================================================================
# MICROSERVICES (gRPC URLs for API Gateway)
# =============================================================================
USER_SERVICE_URL=localhost:50051
PAYMENT_SERVICE_URL=localhost:50052
AUTH_SERVICE_URL=localhost:50053

# =============================================================================
# MESSAGE BROKER
# =============================================================================
RABBITMQ_URL=amqp://admin:admin@localhost:5672

# =============================================================================
# AUTHENTICATION
# =============================================================================
# JWT secret for signing access tokens (REQUIRED - use a strong random string)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# =============================================================================
# OAUTH PROVIDERS (optional - for social login)
# =============================================================================

# Google Sign-In
# Get credentials from: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID (Web application type)
# 2. Add authorized JavaScript origins (e.g., http://localhost:3000)
# 3. Copy the Client ID here
GOOGLE_CLIENT_ID=your-google-client-id

# Apple Sign In
# Get credentials from: https://developer.apple.com/account/resources/identifiers
# 1. Create an App ID with Sign In with Apple capability
# 2. Create a Services ID for web authentication
# 3. Copy the Services ID identifier here (e.g., com.yourcompany.yourapp.service)
APPLE_CLIENT_ID=your-apple-client-id

# =============================================================================
# EMAIL (optional - for production)
# =============================================================================
# SendGrid API key for sending emails
# Get from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=your-sendgrid-api-key

# Email sender configuration
EMAIL_FROM="Your App" <noreply@yourapp.com>

# Frontend URL (used in email templates for links)
FRONTEND_URL=http://localhost:3000

# Company name (used in email templates)
COMPANY_NAME=Your Company Name

# =============================================================================
# DEVELOPMENT
# =============================================================================
NODE_ENV=development
LOG_LEVEL=debug
